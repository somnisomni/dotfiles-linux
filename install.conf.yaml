- defaults:
    link:
      create: true
      relink: true

- clean: [ '~/' ]

- link:
    # Git
    ~/.gitignore: git/.gitignore
    ~/.gitconfig: git/.gitconfig

    # ZSH
    ~/.zshrc: zsh/.zshrc
    ~/.zprofile: zsh/.zprofile
